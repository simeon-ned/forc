{
  "name": "forc-site",
  "private": true,
  "version": "0.1.0",
  "description": "Fundamentals of Robot Control — course site (Eleventy)",
  "scripts": {
    "build": "eleventy",
    "dev": "CHOKIDAR_USEPOLLING=1 eleventy --serve --port=8080",
    "dev:native": "eleventy --serve --port=8080",
    "clean": "rm -rf _site"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it-anchor": "^9.2.0"
  }
}
